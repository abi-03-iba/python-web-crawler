get_database-->connect to the specific db called webscrap
validate()-->validate an URL by checking whether a particular url returns a valid response or not
insertedalready()-->function to check whether particular link already exists or not
find_all('a')-->extract all anchor tags
scrape() -->used for scrpping the links
urlopen()-->for opening the url which we request
ThreadPoolExecutor()-->thread pool will only have  concurrent threads that can process jobs
